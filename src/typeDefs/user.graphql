type User {
  id: Int!
  name: String!
  email: String!
}

type Query {
  user: String
}

type Mutation {
  createUser(input: UserInput): User!
}

input UserInput {
  name: String!
  email: String!
}
